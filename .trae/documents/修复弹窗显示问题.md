1. **替换未使用自定义弹窗的原生调用**：
   - 修改 `components/Modals.tsx` 第75行，将恢复默认设置的 `confirm()` 替换为 `showConfirm()`
   - 修改 `components/Modals.tsx` 第129行，将切换提示词时的 `confirm()` 替换为 `showConfirm()`
   - 在 `Modals.tsx` 中引入 `useAlert` 钩子

2. **检查弹窗组件集成**：
   - 确保所有组件都正确使用 `useAlert` 钩子获取 `showAlert` 和 `showConfirm` 方法
   - 检查 `CustomAlert.tsx` 中的状态管理逻辑，确保弹窗能正确显示和关闭
   - 验证 `AlertProvider` 在 `index.tsx` 中正确包裹了整个应用

3. **测试弹窗功能**：
   - 测试提示词管理中的恢复默认功能
   - 测试提示词管理中的切换未保存修改功能
   - 测试其他已有弹窗功能，确保它们都能正常显示

4. **优化弹窗样式和体验**：
   - 确保所有弹窗样式统一，符合应用的设计风格
   - 验证不同类型弹窗（success, error, warning, info）的样式是否正确
   - 测试自动关闭功能是否正常工作

5. **确保Promise支持**：
   - 验证 `showConfirm` 方法的Promise返回值是否正确处理
   - 确保异步操作中的弹窗能正确触发回调函数

通过以上步骤，我将确保所有弹窗都能正常显示，并且使用统一的自定义弹窗组件，提供更好的用户体验。