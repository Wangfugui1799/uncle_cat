**问题分析**：

* 在 `PromptEditorModal` 组件中，当处于编辑模式（`!isFullPromptView`）时，会显示两个关闭相关的按钮

* "取消" 按钮（第 359-361 行）：调用 `onClose()`

* "关闭" 按钮（第 374-376 行）：调用 `onClose()`

* 这两个按钮的功能完全相同，都是关闭模态框，所以确实存在重复

* 而当处于完整提示词视图（`isFullPromptView`）时，只显示"复制完整提示词"和"关闭"按钮，没有"取消"按钮，这是合理的

**解决方案**：

* 移除编辑模式下的"取消"按钮，只保留"关闭"按钮

* 无论处于哪种视图模式，都只显示一个关闭按钮

* 保持功能的简洁性和一致性

**修改内容**：

1. **移除编辑模式下的"取消"按钮**：

   * 删除第 358-362 行的"取消"按钮代码

   * 只保留第 374-376 行的"关闭"按钮

**修改位置**：

* `components/Modals.tsx`：`PromptEditorModal` 组件的按钮区域

**预期效果**：

* 当处于编辑模式（`!isFullPromptView`）时，显示"保存修改"和"关闭"按钮

* 当处于完整提示词视图（`isFullPromptView`）时，显示"复制完整提示词"和"关闭"按钮

* 移除了重复的"取消"按钮，保持了界面的简洁性

* 功能保持不变，用户仍然可以关闭模态框

* 提升了用户体验，减少了用户的困惑

**技术要点**：

* 保持与现有代码结构的一致性

* 移除冗余的按钮，简化界面

* 确保功能不受影响

