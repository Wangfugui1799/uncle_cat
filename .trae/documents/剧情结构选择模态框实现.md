1. **创建新模态框组件**：在`components/Modals.tsx`中添加`PlotStructureModal`组件，实现剧情结构的可视化选择功能

2. **组件功能**：
   - 模态框标题为"选择剧情结构"
   - 顶部搜索栏，可按名称或描述搜索剧情结构
   - 每个剧情结构显示为卡片，包含中文名称（英文名称）和详细描述
   - 点击卡片即可选择对应结构
   - 统一剧情结构显示格式为中文名称（英文名称）

3. **修改App.tsx**：
   - 添加模态框状态管理：`showPlotStructureModal`
   - 添加模态框打开和关闭事件处理函数
   - 修改顶部导航栏中的剧情结构选择，改为点击打开模态框
   - 更新`selectedPlotStructure`状态管理

4. **优化UI设计**：
   - 采用深色主题，符合项目整体设计风格
   - 卡片悬停效果和选中状态
   - 滚动条样式统一
   - 响应式设计，适配不同屏幕尺寸

5. **确保数据一致性**：
   - 保持`PLOT_STRUCTURES`常量不变，在组件中进行格式化显示
   - 确保选中的剧情结构正确传递给生成函数

6. **增强用户体验**：
   - 搜索结果实时更新
   - 卡片点击反馈
   - 模态框动画效果
   - 清晰的视觉层次

7. **测试和调试**：
   - 确保模态框正常打开和关闭
   - 确保剧情结构选择正确
   - 确保搜索功能正常工作
   - 确保生成函数正确接收所选结构

这个实现将改进现有的剧情结构选择功能，提供更直观、更友好的用户体验，同时保持项目的整体设计风格一致性。