1. **修改弹窗默认自动关闭设置**：
   - 修改 `CustomAlert.tsx` 第202行，将 `showAlert` 函数的默认 `duration` 从 `3000` 改为 `0`
   - 确保所有弹窗需要用户主动关闭，而不是自动消失
   - 保留 `showConfirm` 函数的 `duration: 0` 设置不变

2. **测试修改后的弹窗行为**：
   - 构建项目，确保没有编译错误
   - 启动预览服务器，测试所有弹窗功能
   - 验证弹窗不会自动关闭，需要用户主动点击关闭按钮

3. **确保弹窗功能完整性**：
   - 测试提示词管理中的恢复默认功能
   - 测试提示词管理中的切换未保存修改功能
   - 测试其他弹窗功能，确保它们都能正常工作

通过以上修改，所有弹窗都将需要用户主动关闭，符合用户的预期行为，提高了用户体验。