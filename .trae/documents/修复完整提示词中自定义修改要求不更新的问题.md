1. **添加状态变量**：在App.tsx中添加`stepCustomInstructions`状态变量，用于存储每个步骤的最新修改意见
2. **更新提交逻辑**：修改openCustomModal函数的回调处理，在调用handleGenerateStep前保存修改意见到状态中
3. **修改完整提示词生成**：在handleShowPrompt函数中，使用对应步骤的实际修改意见生成完整提示词
4. **清理临时数据**：在handleGenerateStep函数完成后，清空对应步骤的修改意见

这个方案将确保完整提示词能够显示实际使用的自定义修改要求，保持与生成内容的一致性。