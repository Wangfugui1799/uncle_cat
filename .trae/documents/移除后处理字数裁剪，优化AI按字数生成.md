**问题分析**：
用户反馈字数控制准确了，但文章被截断不完整。当前实现使用后处理裁剪导致内容不完整，用户希望让AI直接按字数生成完整内容，而不是手动截断。

**优化方案**：

1. **移除后处理字数裁剪**：删除App.tsx中的内容裁剪逻辑
2. **优化提示词**：增强提示词中的字数要求，明确要求AI生成完整内容
3. **优化max\_tokens计算**：精确计算max\_tokens，确保AI能生成符合要求的完整内容
4. **优化API请求参数**：为不同模型设置更合适的参数

**修改点**：

1. **文件**：`App.tsx`

   * 函数：`handleGenerateChapter`

   * 内容：移除后处理字数裁剪逻辑

2. **文件**：`constants.ts`

   * 内容：优化章节提示词，增强字数要求的明确性

3. **文件**：`services/apiService.ts`

   * 函数：`buildRequestBody`

   * 内容：优化max\_tokens计算，确保生成完整内容

**预期效果**：

* AI直接按用户要求的字数生成完整内容

* 生成的内容不会被截断

* 字数控制准确（±5%误差）

* 保持内容完整性

