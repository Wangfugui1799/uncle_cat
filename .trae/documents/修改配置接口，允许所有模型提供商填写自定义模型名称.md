1. **修改模型预设配置**：
   - 修改 `MODEL_PRESETS` 对象，为每个模型提供商（google, openai, claude, deepseek, custom）的 models 数组添加一个 `'custom'` 选项
   - 确保每个模型列表都有一个 "自定义" 选项，允许用户选择自定义模型

2. **调整自定义模型输入框显示逻辑**：
   - 保持现有的 `localConfig.textModel === 'custom'` 条件不变
   - 当用户选择 "自定义" 选项时，显示自定义模型名称输入框
   - 确保输入框的样式和功能保持一致

3. **测试修改后的配置界面**：
   - 构建项目，确保没有编译错误
   - 启动预览服务器，测试配置接口
   - 验证所有模型提供商都能选择 "自定义" 选项并填写自定义模型名称

通过以上修改，用户将能够在所有模型提供商的配置中选择自定义模型，并填写自定义模型名称，满足用户的需求。