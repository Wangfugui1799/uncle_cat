1. 找到创作初始化页面中预计章节数和每章字数的输入框代码
2. 修改onChange事件处理函数，将`parseInt(e.target.value) || defaultValue`改为`parseInt(e.target.value) || prev.currentValue`
3. 这样当用户删除输入框内容时，输入框会保持当前值，而不是自动恢复为默认值
4. 允许用户自由删除和输入新的数字，提升用户体验

