### 修复章节蓝图提取问题

**问题分析**：
1. 蓝图实际格式为 `### 第X章 - [章节标题]`，当前提取逻辑可能无法正确匹配
2. 章节内容提取正则可能存在边界问题
3. 提取逻辑需要更鲁棒，能处理各种格式变体

**修复方案**：

1. **重新设计章节内容提取逻辑**：使用更可靠的分割方法替代正则匹配
2. **优化标题提取**：针对 `### 第X章 - 标题` 格式设计专门的提取逻辑
3. **增强提取鲁棒性**：添加更多格式处理和容错机制
4. **完善调试日志**：添加关键节点的调试信息

**文件修改**：
- `components/WritingStep.tsx`：重写蓝图提取逻辑，使用更可靠的分割和匹配方法

**预期效果**：
- 能够正确从蓝图中提取章节标题
- 能够正确提取本章定位信息
- 提取逻辑更加鲁棒，能处理各种格式变体
- UI显示与用户预期一致