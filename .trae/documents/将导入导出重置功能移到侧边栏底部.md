1. **移除当前的固定定位按钮**：
   - 删除main元素中固定定位的导入导出重置按钮代码 (1763-1788行)
   - 保留handleExport、handleImport、handleReset和相关状态变量

2. **在侧边栏底部添加按钮**：
   - 在侧边栏的步骤卡片区域和版本信息区域之间添加导入导出重置按钮
   - 位置：<div className="flex-1 overflow-y-auto p-4 space-y-2"> 之后，<div className="p-4 border-t border-stone-800 text-xs"> 之前
   - 使用侧边栏的样式风格：深色背景、圆角设计、适合侧边栏的布局

3. **按钮样式设计**：
   - 使用侧边栏的背景色和边框样式
   - 按钮宽度占满侧边栏
   - 图标+文字组合，保持一致性
   - 使用与侧边栏其他元素相同的字体大小和间距

4. **代码实现**：
   - 在App.tsx中移除固定定位的按钮
   - 在侧边栏底部添加新的按钮代码
   - 保持原有的handleExport、handleImport、handleReset函数不变

5. **测试验证**：
   - 构建项目，确保没有编译错误
   - 启动预览服务器，测试新的布局
   - 验证按钮功能正常，布局符合预期

通过以上修改，导入导出重置功能将从固定定位的左下角移动到侧边栏底部的红框位置，符合用户的需求。