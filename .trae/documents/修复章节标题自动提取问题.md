1. **添加状态变量**：在WritingStep组件中添加`manuallyEditedTitles`状态变量，用于跟踪哪些章节的标题已被手动编辑
2. **修改标题保存逻辑**：在`handleTitleSave`函数中，将当前章节添加到`manuallyEditedTitles`集合中
3. **调整标题提取逻辑**：在useEffect中，检查当前章节是否已被手动编辑

   * 如果已手动编辑，保留现有标题

   * 如果未手动编辑，从蓝图中提取最新标题
4. **重置手动编辑状态**：当切换章节或蓝图更新时，根据需要重置手动编辑状态

