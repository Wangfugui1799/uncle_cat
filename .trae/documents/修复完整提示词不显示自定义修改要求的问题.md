1. **分析问题**：当生成完成后，代码会立即清空stepCustomInstructions，导致查看提示词时无法获取实际使用的修改要求

2. **修复方案**：
   - 移除handleGenerateStep函数中清空stepCustomInstructions的代码
   - 保持自定义修改要求在state中，直到用户关闭提示词模态框或切换步骤
   - 确保查看提示词时能获取到实际使用的修改要求

3. **实现步骤**：
   - 修改App.tsx文件，删除handleGenerateStep函数中的stepCustomInstructions清空逻辑
   - 确保custom_instruction变量在完整提示词生成时能正确获取到修改要求
   - 测试修复后的功能是否正常

4. **预期效果**：
   - 查看提示词时，完整提示词中会显示实际使用的自定义修改要求
   - 用户能看到AI实际接收的完整指令
   - 保持原有功能的完整性

这个方案将确保完整提示词始终显示实际使用的自定义修改要求，提高用户体验和透明度。