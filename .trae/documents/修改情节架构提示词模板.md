1. **定位PLOT提示词模板**：在constants.ts文件中找到当前的PLOT提示词模板，位于PROMPTS对象中

2. **替换为新模板**：将当前模板完整替换为用户提供的新提示词，确保以下几点：
   - 保持变量占位符不变：{STORY_DNA}, {character_dynamics}, {world_building}, {custom_requirements}, {custom_instruction}, {plot_structure}
   - 更新中文标签：将"输出格式请基于以下要素"改为"请基于以下要素设计情节架构"
   - 调整要素顺序：核心DNA → 角色体系 → 世界观 → 自定义特殊要求 → 自定义修改要求 → 选定剧情结构
   - 更新标签名称：将"STOYR_DNA"改为"核心DNA"，"character_dynamics"改为"角色体系"，"world_building"改为"世界观"
   - 保持代码块格式不变，确保反引号正确转义

3. **验证修改**：确保修改后的提示词格式正确，变量占位符完整，没有语法错误

4. **测试构建**：运行构建命令，确保修改不会导致构建失败

这个修改将更新情节架构生成的提示词，使其更符合用户需求，同时保持与现有代码的兼容性。