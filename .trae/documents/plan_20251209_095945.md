## 修复计划

### 1. 问题分析
- **核心问题**：判官审题后生成的内容只显示核心DNA，不显示基础设定，而直接生成和重新生成的内容显示正常
- **根本原因**：`handleSelectJudgeProposal`函数只将核心DNA部分设置到`generatedData.dna`中，而完整的AI生成结果（包括基础设定）被丢弃

### 2. 修复方案
- 修改`handleSelectJudgeProposal`函数，将完整的AI生成结果（包括基础设定和核心DNA）设置到`generatedData.dna`中
- 确保`parseGeneratedResult`函数正确解析AI生成的结果，提取完整内容
- 保持与`handleGenerateStep`函数一致的处理逻辑

### 3. 修复步骤
1. 修改`App.tsx`中的`handleSelectJudgeProposal`函数，将完整AI生成结果保存到`generatedData.dna`
2. 执行构建命令验证修复
3. 启动预览服务器测试功能